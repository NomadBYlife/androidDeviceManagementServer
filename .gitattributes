# Make .sh files always have LF line endings on checkout
*.sh text eol=lf
makefile text eol=lf

# Make .bat files always have CRLF line endings on checkout
# *.bat text eol=crlf

# Make this particular .bat file have lf line endings because it is a very special snowflake
# this rule has to be below the previous rule
*cyg_script.bat text eol=lf
